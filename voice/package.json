{
  "name": "@smithagents/voice",
  "version": "0.1.0",
  "type": "module",
  "description": "VoiceProvider abstraction: local (Piper/MLX), ElevenLabs (BYO key), and a pre-cache decorator. Same interface, chosen by policy.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "format": "biome format --write ."
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.3",
    "@types/node": "^24.13.3",
    "typescript": "~6.0.0"
  }
}
