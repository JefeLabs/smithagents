<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <!-- Root reactor (aggregator). Inherits Spring Boot for dependency + plugin
       management, but the spring-boot-maven-plugin is bound ONLY in the gateway
       module so the library modules aren't repackaged as executable jars. -->
  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>4.1.0</version>
    <relativePath/>
  </parent>

  <groupId>com.jefelabs</groupId>
  <artifactId>smithagents-parent</artifactId>
  <version>0.1.0</version>
  <packaging>pom</packaging>
  <name>smithagents-parent</name>
  <description>Reactor for the smithagents swarm monorepo (PRD).</description>

  <!-- Reactor build order follows dependency order: libs before the app. -->
  <modules>
    <module>personas</module>
    <module>agent-robot</module>
    <module>gateway</module>
  </modules>

  <properties>
    <java.version>21</java.version>
    <!-- Embabel's Spring Boot 4 line is the UNRELEASED 2.0.0 branch, published
         only to Embabel's Artifactory snapshots (see <repositories> below) —
         NOT on Maven Central. Bleeding edge: snapshot builds can change or break
         without notice. Baseline is Spring Boot 4.0.6 + Spring AI 2.0.0-M8; we
         run it on 4.1. Pin to a timestamped snapshot if you need reproducibility. -->
    <embabel.version>2.0.0-SNAPSHOT</embabel.version>
    <!-- TODO: confirm latest JDA 5.x on Maven Central before first build. -->
    <jda.version>5.2.1</jda.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <!-- Internal modules: version pinned once, consumed without versions. -->
      <dependency>
        <groupId>com.jefelabs</groupId>
        <artifactId>personas</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>com.jefelabs</groupId>
        <artifactId>agent-robot</artifactId>
        <version>${project.version}</version>
      </dependency>
      <!-- Third-party pinned here so modules don't repeat versions. -->
      <dependency>
        <groupId>com.embabel.agent</groupId>
        <artifactId>embabel-agent-starter</artifactId>
        <version>${embabel.version}</version>
      </dependency>
      <dependency>
        <groupId>net.dv8tion</groupId>
        <artifactId>JDA</artifactId>
        <version>${jda.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <!-- Embabel 2.0.0-SNAPSHOT lives in Embabel's Artifactory (not Central);
       Spring AI 2.0.0-M8, pulled transitively, is a Spring milestone. -->
  <repositories>
    <repository>
      <id>embabel-snapshots</id>
      <name>Embabel Artifactory Snapshots</name>
      <url>https://repo.embabel.com/artifactory/libs-snapshot</url>
      <releases><enabled>false</enabled></releases>
      <snapshots><enabled>true</enabled></snapshots>
    </repository>
    <repository>
      <id>embabel-releases</id>
      <name>Embabel Artifactory Releases</name>
      <url>https://repo.embabel.com/artifactory/libs-release</url>
      <releases><enabled>true</enabled></releases>
      <snapshots><enabled>false</enabled></snapshots>
    </repository>
    <repository>
      <id>spring-milestones</id>
      <name>Spring Milestones</name>
      <url>https://repo.spring.io/milestone</url>
      <snapshots><enabled>false</enabled></snapshots>
    </repository>
  </repositories>
  <pluginRepositories>
    <pluginRepository>
      <id>spring-milestones</id>
      <name>Spring Milestones</name>
      <url>https://repo.spring.io/milestone</url>
      <snapshots><enabled>false</enabled></snapshots>
    </pluginRepository>
  </pluginRepositories>
</project>
